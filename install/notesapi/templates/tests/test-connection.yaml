apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notesapi.fullname" . }}-test-connection"
  labels:
    {{- include "notesapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notesapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
